# CURRENT_STATUS.md

## Статус проекта на 2026-04-27

Текущие изменения вынесены в draft PR:

- PR: `https://github.com/Serebubi/sarmaexpress/pull/1`
- Репозиторий: `https://github.com/Serebubi/sarmaexpress`
- База: `main`
- Ветка: `codex/calculator-marketplaces-ftl-ltl`

Локальная папка `C:\Users\Dell\Documents\Codex\sarmaexpress-main` не является git-репозиторием. Для отправки PR был создан клон:

- `C:\Users\Dell\Documents\Codex\sarmaexpress-pr-work`

## Что завершено

### Калькулятор

Файлы:

- `frontend/components/delivery-calculator-page.tsx`
- `frontend/lib/delivery-tariffs.ts`
- `shared/src/index.ts`
- `backend/src/routes/orders.ts`

Сделано:

- В городах выбора `Откуда` и `Куда` теперь три отдельные кнопки: `ПВЗ`, `Склад`, `Курьер`.
- Доступность кнопок зависит от города, типа точки и лимита веса.
- Если выбран ПВЗ, но вес превышает лимит, логика переводит расчёт на склад, если склад есть.
- Если вес превышает лимит ПВЗ и склада нет, автоматически выбирается `Курьер`, а кнопка ПВЗ становится неактивной.
- Для Москвы включён спецсценарий с заявкой/звонком и направлением от 500 кг.
- Для веса от 500 кг справа показывается информация о спецтарифе и кнопки `Заполнить заявку` / `Позвонить нам`.
- Добавлена анкета оформления заказа после расчёта.
- Исправлено наложение анкеты и расчёта: форма переключается в панели, а не накладывается поверх.
- Поле `Объём` заменено на `Длина`, `Ширина`, `Высота`.
- Добавлен красивый кастомный выбор типа транспорта в анкете.

### Пункты выдачи и карта

Файлы:

- `shared/src/index.ts`
- `frontend/components/pickup-points-page.tsx`

Сделано:

- Добавлены адреса ПВЗ и складов из локального файла `Новая таблица (1).xlsx`.
- Эти точки используются в разделе выбора пункта выдачи и на странице карты `/pickup-points`.
- На карте и в списке отображаются адреса и график работы.

### Superbox и маркетплейсы

Файл:

- `frontend/components/superbox-app.tsx`

Сделано:

- Убраны сценарии `WB опт` и `WB дорогостой`.
- Устранён визуальный пробел после удаления карточек маркетплейсов.
- Плашка выбранного сценария с кнопкой `Продолжить` стала фиксированной снизу экрана.
- Для OZON и Wildberries в анкетах убраны поля:
  - `Количество товаров`
  - `Итоговая цена всех товаров`
- В shared-схемах учтена новая логика обязательных полей для OZON/Wildberries.

### Страницы FTL и LTL

Файлы:

- `frontend/app/ftl/page.tsx`
- `frontend/components/ftl-page.tsx`
- `frontend/app/ltl/page.tsx`
- `frontend/components/ltl-page.tsx`
- `frontend/components/sarma-express-page.tsx`

Сделано:

- Создана страница `/ftl` по файлу `Страница FTL — Полная загрузка.xlsx`.
- Создана страница `/ltl` по файлу `ТЗ_ Страница — Сборные грузы от 500 кг.xlsx`.
- Карточка `FTL - ПОЛНАЯ ЗАГРУЗКА` ведёт на `/ftl`.
- Карточка `LTL - СБОРНЫЕ ГРУЗЫ` ведёт на `/ltl`.
- Устранена React-ошибка duplicate key на FTL-странице.
- Проверено в браузере: `/ltl` открывается, форма отправляется, консоль без ошибок.

## Проверки

В чистом клоне PR выполнено:

- `npm ci`
- `npm run build -w frontend`
- `npm run build -w backend`

Сборки проходят.

Замечания:

- `npm ci` показывает npm audit предупреждения: `3 moderate`, `4 high`.
- `npm run lint -w frontend` сейчас падает на существующих React lint-ошибках:
  - `frontend/components/delivery-calculator-page.tsx:273`
  - `frontend/components/superbox-app.tsx:775`
- Эти lint-ошибки не блокируют production build, но их стоит исправить отдельной задачей.

## Что осталось / риски

- PR открыт как draft, его нужно перевести в ready-for-review или смерджить после проверки владельцем.
- Локальные Excel-файлы с ТЗ не добавлялись в Git.
- Файлы `frontend-start-*.log` в рабочей папке являются локальными логами и не должны попадать в Git.
- В рабочей папке есть несколько поднятых ранее Next-портов, актуальный проверочный порт был `3016`.

## Точка входа для следующего агента

Если нужно продолжать работу с PR:

1. Перейти в `C:\Users\Dell\Documents\Codex\sarmaexpress-pr-work`.
2. Проверить `git status -sb`.
3. Ветка должна быть `codex/calculator-marketplaces-ftl-ltl`.
4. При новых изменениях коммитить в эту ветку и пушить в тот же PR.

Если нужно работать с локальной рабочей папкой пользователя:

1. Перейти в `C:\Users\Dell\Documents\Codex\sarmaexpress-main`.
2. Помнить, что это не git-репозиторий.
3. После правок при необходимости переносить изменённые файлы в `sarmaexpress-pr-work`.
