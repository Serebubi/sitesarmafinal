# HANDOFF.md

## Короткая передача состояния

Текущий фокус проекта: привести публичную навигацию `Сарма Экспресс` и ключевые `SUPERBOX flow` к единому стилю.

Ветка перед фиксацией: `codex/ver7`.

## Самое важное

- Главная страница - `Сарма Экспресс`.
- `SUPERBOX` остается на `/superbox`, но часть его flow уже визуально сделана под Sarma.
- Все переходы лучше делать через существующие `flow`, если это возможно.

## Последние важные изменения

1. В верхней плашке добавлена отдельная кнопка `Тарифы`.
2. `Бизнесу` теперь ведет на `/superbox?flow=business`.
3. `/superbox?flow=business` показывает пустую страницу `скоро добавим)`.
4. `Тарифы` ведут на `/superbox?flow=tariffs`.
5. Все 4 карточки услуг на главной ведут на `/superbox?flow=pickup_paid`.
6. Адрес ПВЗ изменен на `Мариуполь, улица 60 лет СССР`.
7. `pickup_paid` и `pickup_standard` шаги выбора и анкеты переведены в Sarma-стиль.
8. Dropdown ПВЗ теперь не вылезает за границы экрана: ограничивает высоту и открывается вверх при нехватке места снизу.

## Что смотреть первым

- `frontend/components/sarma-express-header.tsx` - верхнее меню.
- `frontend/components/sarma-express-page.tsx` - главная и карточки услуг.
- `frontend/components/superbox-app.tsx` - все Superbox flow.
- `frontend/components/delivery-calculator-page.tsx` - калькулятор.
- `frontend/components/pickup-points-page.tsx` - пункты выдачи и карта.
- `shared/src/index.ts` - общие адреса ПВЗ.

## Возможные следующие задачи

- Довести финальные success-экраны Superbox до Sarma-стиля.
- Доработать анкеты отдельных маркетплейсов после визуальной проверки пользователем.
- Настроить реальные формулы калькулятора, когда пользователь даст тарифную логику.
- Продолжить точечные правки верхнего меню и ссылок.

## Осторожно

- Не откатывать изменения в dirty worktree без явной просьбы.
- Не удалять пользовательские ассеты.
- Не трогать backend/Bitrix для чисто UI-задач.
- `shared/dist` может быть не синхронизирован с `shared/src`; исходником считать `shared/src/index.ts`, если сборка не потребует иначе.
